# Scraper environment (copy to .env and fill in).
#
# There is nothing required. This service is a jobspy adapter: search
# parameters in, listings out. It connects to no database, calls no API and
# resolves no identity, so it holds no credential at all.
#
# Optional (default shown)
# CORS_ORIGINS=*
