{
  "_comment": "LOCAL DEVELOPMENT ONLY -- one small board, so a local ingest run costs cents, not dollars. The root docker-compose.yml points both Greenhouse services here by default (GREENHOUSE_COMPANIES_CONFIG overrides it); production reads companies.json, via deploy/compose.yml, which never mentions this file. Why it exists: on 2026-09-22 a day of local runs over every board cost $6.25 on the local key, two thirds of that week's Anthropic bill. similarweb is the smallest of the production boards (66 postings measured 2026-09-23), so a full run here is roughly $0.45 live or $0.25 through batches. Do NOT shrink a board by capping postings instead: the close diff would close every posting beyond the cap.",
  "companies": [
    "similarweb"
  ],
  "company_domains": {
    "similarweb": "similarweb.com"
  },
  "_served_locations_comment": "Where the product's users are, for the pre-read filter (Greenhouse__Prefilter=log|on): a NEW posting whose board location and offices name none of these is not read, embedded or stored. Whole words, case-insensitive, against the board's own free text -- so list cities as well as countries: a posting that says only 'Herzliya' is otherwise ruled out. 'Remote' reads every remote posting, including US-only ones; kept because a wrong skip is silent and an extra read is not. Empty list = no location filtering.",
  "served_locations": ["Israel", "Tel Aviv", "Tel Aviv-Yafo", "Herzliya", "Haifa", "Jerusalem", "Petah Tikva", "Ra'anana", "Ramat Gan", "Netanya", "Rehovot", "Be'er Sheva", "Beersheba", "London", "United Kingdom", "UK", "England", "Remote"],

  "embed_batch_token_budget": 100000,
  "max_batch_items": 128
}
